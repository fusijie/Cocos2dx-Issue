###dispatchCustomEvent-crash

---

`version:` v3.3

`Issue:` if dispatch the same event on the listener's event callback, the program will crash.

`reproduce:`

download the 2 classes(HelloWorldScene & BugUnit) to v3.3 HelloWorld Project.

